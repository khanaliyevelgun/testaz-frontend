# Backend API base URL.
# Leave empty only when the backend is served from the same origin as the Next.js app.
NEXT_PUBLIC_API_BASE_URL=https://wildcard-chimp-vanquish.ngrok-free.dev

# Highest grade shown in admin grade selectors. Defaults to 11 when omitted.
NEXT_PUBLIC_MAX_GRADE=11
